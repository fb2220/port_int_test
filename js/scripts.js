// Just an example to simulate job status
setTimeout(() => {
    document.getElementById('job-status').textContent = 'Success';
    document.getElementById('job-status').style.color = 'green';
}, 3000); // After 3 seconds, change status to "Success"
